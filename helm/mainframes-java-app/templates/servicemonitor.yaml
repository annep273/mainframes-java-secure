{{- if .Values.monitoring.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "mainframes-java-app.fullname" . }}
  labels:
    {{- include "mainframes-java-app.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "mainframes-java-app.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: http
    path: {{ .Values.monitoring.serviceMonitor.path }}
    interval: {{ .Values.monitoring.serviceMonitor.interval }}
{{- end }}
